> Создайте скрипт sleeper.sh, который будет 10 раз с интервалом в 5 секунд
писать дату в формате HH:MM:SS и количество процессов одним числом 

// Сначала хотела через top, потом разобралась - правильно date ps -ef | wc -l - но почему-то в 1 строку не получается? Что не так? 


ssh -i ~/.ssh/id_rsa ec2-user@linux.telran-edu.de
cd /opt/43_44_morning/Yuliia
ls

1 nano script.sh
2 #!/bin/bash

for run in {1..10}

do

echo $run
date ps -ef | wc -l

sleep 5

done

3 chmod 744 sleeper.sh
4 ./sleeper.sh


> Уменьшите или уберите временной интервал (который нам дает sleep), используя vi или nano, 
закомментировав строку или поменяв значение sleep

1 nano sleeper.sh
2 Меняем sleep 5 на sleep 2 

> С помощью скрипта запишите в файл информацию об операционной системе, 
но отфильтруйте информацию так, чтобы осталось только имя (NAME=Alpine Linux).

1 cat /etc/os-release | grep NAME head -1 >> result.txt // По идее, между do и done нужно куда-то всунуть эту строку, 
но я не понимаю куда..выдает ошибку**(( //help please

> Выполните прошлое действие, но так, чтобы слово NAME= не осталось, а было только имя в чистом виде (Alpine)
1 cat /etc/os-release | grep NAME head -1 | cut -d '=' -f 2 >> result.txt

> С помощью скрипта создайте 50 файлов с расширением txt и именами от 50.txt до 100.txt

#!/bin/bash

for run in {50..100}

do

echo $run
date
touch txt_$run
sleep 2

done


